machine:
  pre:
    - sudo -i
    

checkout:
  post:
    - sudo git clone git@github.com:GERey/pixelgroup-home.git

test:
  override:
    - echo "party"




#version: 2
#jobs:
#  build:
#    working_directory: ~/test
#    machine: true
#    
#    steps:
#      - run: sudo apt-get install openvpn-client
    
